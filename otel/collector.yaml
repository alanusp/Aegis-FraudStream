# SPDX-License-Identifier: Apache-2.0
receivers:
  otlp:
    protocols:
      http:
exporters:
  logging: {}
  otlp:
    endpoint: "otel.example.com:4318"
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [logging, otlp]
    metrics:
      receivers: [otlp]
      exporters: [logging, otlp]
