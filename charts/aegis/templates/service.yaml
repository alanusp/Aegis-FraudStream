# SPDX-License-Identifier: Apache-2.0
apiVersion: v1
kind: Service
metadata: { name: {{ include "aegis.fullname" . }} }
spec:
  selector: { app: {{ include "aegis.name" . }} }
  ports: [ { name: http, port: {{ .Values.service.port }}, targetPort: 8080 } ]
