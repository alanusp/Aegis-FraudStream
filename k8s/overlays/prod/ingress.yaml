# SPDX-License-Identifier: Apache-2.0
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: { name: aegis }
spec:
  rules:
  - host: aegis.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend: { service: { name: aegis-svc, port: { number: 80 } } }
